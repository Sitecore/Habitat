@using Sitecore.Feature.News
@using Sitecore.Foundation.Dictionary
@using Sitecore.Foundation.SitecoreExtensions.Extensions
@model IEnumerable<Sitecore.Data.Items.Item>
<div class="well bg-primary">
  <h5 class="text-uppercase">@Html.Sitecore().DictionaryField("/News/Latest News/Title", "News")</h5>
  <ul class="media-list">
    @foreach (var item in Model)
    {
      <li class="media">
        <div class="media-body">
          <date>
            @Html.Sitecore().Field(Templates.NewsArticle.Fields.Date, item, new
             {
               format = "MMMM dd, yyyy"
             })
          </date>
          <h4 class="media-heading">
            <a href="@item.Url()">@Html.Sitecore().Field(Templates.NewsArticle.Fields.Title, item)</a>
          </h4>
        </div>
      </li>
    }
  </ul>
  <a href="@Html.Sitecore().CurrentRendering.Item.Url()" class="btn btn-default">
    @Html.Sitecore().DictionaryField("/News/Latest News/Read More", "Read more")
  </a>
</div>